{
  "name": "hour-organizator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate-bat": "echo @echo off > run_dev.bat && echo start \"\" \"http://localhost:5172\" > run_dev.bat && echo npm run dev -- --host --port 5172 >> run_dev.bat"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  }
}
